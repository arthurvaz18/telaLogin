spring:
  application:
    name: telaLogin
  datasource:
    url: jdbc:postgresql://localhost:5436/telaLogin
    username: telaLogin
    password: telaLogin
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update